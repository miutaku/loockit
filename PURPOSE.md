SESAME4 / Bot 自動化システム 要件定義書

# 1. 機能要件 (Functional Requirements)

## 1.1. デバイス制御・通信レイヤー
ローカル直接制御 (BLE - SesameOS2仕様)

Wi-Fiモジュールを介さず、コンテナを実行するホストのBluetoothインターフェースから直接デバイスを制御できること。

SesameOS2の暗号化プロトコル（AES-128-GCM、またはコマンド生成時のランダムコード交換ロジック）を実装すること。

アプリから抽出した Secret Key（16進数文字列）および Public Key を用いて、SesameOS2の仕様に準拠した認証トークンの生成とコマンド暗号化を行うこと。

クラウド制御バックアップ (Web API)

CANDY HOUSE公式の Web API v4（SesameOS2対応API） 経由での制御パスもフォールバックとして利用可能にすること。

## 1.2. 外部インターフェース (API / スマートホーム統合)
REST API / gRPC インタフェース

他の自作システムから、デバイスの「施錠/解錠/Botプッシュ」および「状態取得」をパースしやすい形式で実行できること。

スマートホーム統合 (Matter / Home Assistant)

Matterブリッジ化（第一希望） または Home Assistant / MQTT連携（第二希望）。

※SesameOS2のオープンな知見を活かし、Home Assistant用の既存のカスタムコンポーネント（PySesameOS2等をベースにしたもの）の構造をコンテナ内に内包、またはそれをリファレンスとしてMatterブリッジを構築する。

## 1.3. 状態管理・監視 (State & Monitoring)
リアルタイム状態同期 (SesameOS2パケット解析)

SesameOS2特有のBLEアドバタイズパケット（インクリメンタルなカウントや状態ビットが含まれるペイロード）を常時リスニング・デコードし、手動操作や他アプリからの操作による状態変化（開閉・バッテリー残量）をリアルタイムにシステムへ反映すること。